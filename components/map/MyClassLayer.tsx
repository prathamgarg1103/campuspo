export function MyClassBadge({ index }: { index: number }) {
  return <span className="grid size-6 place-items-center rounded-full border-2 border-white bg-brand-600 text-xs font-bold text-white shadow">{index}</span>
}
